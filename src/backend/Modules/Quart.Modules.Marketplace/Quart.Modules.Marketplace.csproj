<Project Sdk="Microsoft.NET.Sdk">
  <!-- Give-aways, claims, trades and approvals. Built in M7.
       A module owns its own database schema and exposes only what its public MarketplaceModule class exposes.
       It may reference Quart.SharedKernel, never another module (enforced by Quart.ArchitectureTests). -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\Quart.SharedKernel\Quart.SharedKernel.csproj" />
  </ItemGroup>
</Project>
