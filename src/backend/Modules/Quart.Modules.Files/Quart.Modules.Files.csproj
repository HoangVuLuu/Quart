<Project Sdk="Microsoft.NET.Sdk">
  <!-- Private storage, validation by content, image re-encoding, short-lived download links, file access audit. Built in M8.
       A module owns its own database schema and exposes only what its public FilesModule class exposes.
       It may reference Quart.SharedKernel, never another module (enforced by Quart.ArchitectureTests). -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\Quart.SharedKernel\Quart.SharedKernel.csproj" />
  </ItemGroup>
</Project>
