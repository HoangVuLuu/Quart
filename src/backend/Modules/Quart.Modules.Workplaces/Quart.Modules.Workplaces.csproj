<Project Sdk="Microsoft.NET.Sdk">
  <!-- Workplaces, memberships, permissions, levels, join codes, the shift template. Built in M3 and M4.
       A module owns its own database schema and exposes only what its public WorkplacesModule class exposes.
       It may reference Quart.SharedKernel, never another module (enforced by Quart.ArchitectureTests). -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\Quart.SharedKernel\Quart.SharedKernel.csproj" />
  </ItemGroup>
</Project>
